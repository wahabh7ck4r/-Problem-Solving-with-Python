# write a program to input a a nubmer form user and calculate its squar , cube, and squar root.

import math

def find_powers(num):
    sq_root = math.sqrt(num)
    squar = math.pow(num,2)
    cube = math.pow(num,3)
    
